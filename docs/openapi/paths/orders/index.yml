get:
  tags:
    - Order
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../../schemas/order.yml#/schemas/Order"
post:
  tags:
    - Order
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          $ref: "../../schemas/order.yml#/schemas/CreateOrderRequest"
  responses:
    "200":
      description: Success
