get:
  tags:
    - User
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../../schemas/models/user.yml"
post:
  tags:
    - User
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          $ref: "../../schemas/requests/user/create.yml"
  responses:
    "200":
      description: Success
