get:
  tags:
    - User
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../../schemas/user.yml#/schemas/User"
post:
  tags:
    - User
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          $ref: "../../schemas/user.yml#/schemas/CreateUserRequest"
  responses:
    "200":
      description: Success
